---
tags:
  - Python
source: https://wikidocs.net/1015
---
### 집합 자료형은 어떻게 만들까
-------------------------------
집합 자료형은 다음과 같이 set 키워드를 사용해 만든다.
```python
>>> s1 = set([1, 2, 3]) # set()의 괄호 안에 리스트를 입력하여 만들거나
>>> s1
{1, 2, 3}

>>> s2 = set("Hello") # 문자열을 입력하여 만들 수 있다.
>>> s2
{'e', 'H', 'l', 'o'}
# 비어 있는 집합 자료형은 s = set()으로 만들 수 있다. 
```

### 집합 자료형의 특징
----------------------------
> [!info]+ 
> - 중복을 허용하지 않는다.
> - 순서가 없다(Unordered).
> 	- 순서가 없기(unorderd) 때문에 인덱싱을 통해 요소값을 얻을수

